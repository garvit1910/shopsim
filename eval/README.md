# /eval — Calibration & evals (Phase 7, owner: Atishay)

Face-validity suite F1–F15 (F7 and F9 are never-drop — they ARE the demo claim),
aggregate CTR calibration (Criteo/Avazu ranges), rank-agreement vs synthetic
oracle, abstention chart. `make eval` must reproduce every number and plot
from scratch.

Empty until Phase 7.
